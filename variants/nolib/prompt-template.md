Use ./skills/threejs-world-builder/SKILL.md and its routing table, which says what to build for each subject in this brief and which modules to build it from to create this world:

{{BRIEF}}

You are the single builder; no delegation. Aim for convincing, finished scene art in Three.js WebGL2; honor the brief's style, realistic game art when none is stated. Input is this brief, the frozen skills and installed dependencies only: no prior worlds, critiques or local services. Use generic skill judgment; do not force inapplicable capabilities in.

Write the standalone world in ./world/ as index.html plus ES module .js files with bare imports from 'three' and 'three/addons/...' (Three.js 0.185.1 served through Vite); relative asset paths; no dependency changes. Asset policy: procedural only. Geometry and texture data come from code and installed Three.js addons. No external models, scanned or photo textures, HDRIs, image-generated assets, asset packs or preassembled worlds. Write your own reusable modules (render setup, surfaces, construction patterns, figures) into ./world/ and import them relatively. Disclose uncertain historical staging; the brief's recognition anchors are the target.

Validation allowance: Your own CPU/source checks and at most TWO host-rendered inspection requests. Once your world runs, request the first look with: node ./skills/threejs-world-builder/tools/inspect.mjs --views 0,4,5 --times 24 (hero, low contact, action). Before any look and after any camera or placement change, run the free checks: node ./skills/threejs-world-builder/tools/check.mjs (CPU, seconds: finite geometry, deterministic seek, activity, unframed anchors, cameras inside meshes) and node ./skills/threejs-world-builder/tools/inspect.mjs --sanity (host geometry pass incl. interpenetration; no frames, not charged). Ask for more views/times only if you need them; use the second request on the weakest visible regions. Wait for the command to finish, then open the returned native images with view_image and read surfaceAudit/timelineAudit. YOU diagnose and fix; no external critic participates before submission. Do not launch your own browser, server, GPU job or agent. Host capture time is not charged to your allowance. Target your first own visual inspection by 5 minutes elapsed; check the wall clock. This target adds no time or inspection requests. You have up to 15 minutes total; finish within it. Supply exploration controls, pause/reset and activity observable at 0,8,16,24,36,48,64,80 seconds; let actions complete and preserve identities; compressed time is fine if disclosed. Keep scope achievable; spend detail on the closest important objects.

Capture interface (plumbing, not architecture). window.worldTest = {
  ready:true, scene, camera, renderer,            // active THREE camera, THREE.WebGLRenderer
  render?():void,                                  // if you use a post chain, expose its render here; else the harness calls renderer.render(scene,camera)
  views:[{name,position:[x,y,z],target:[x,y,z]}×6], // hero, reverse, side, elevated, low contact, action detail (name contains 'action')
  setView(i), setPaused(bool), setTime(seconds),   // setView updates the camera immediately; setTime seeks both directions deterministically without real-time play
  reset(), stats():{time,phase,actors:[{name,position,state}],objects:[{name,position,state}],camera}  // both arrays nonempty, all numbers finite; time advances after unpause
}. Set shadow maps, materials and color management consistently. Include an inline favicon.

Keep controls and camera targets consistent. Do not edit harness files. Keep test scripts outside world/. Write ./technique-use.json: an array of {skill:"skills/NN-name/SKILL.md",method,file,symbol,check} records for the important techniques actually used (an implementation trace, verified against source and images). End with a concise account of controls, capability applicability, checks, staging and limitations; do not claim rendered quality you have not inspected. Your final response freezes the first submission.